// program to check if a string contains a substring

const str = ('abcdef');
const checkString = ('abcdef');

if(str.includes(checkString)) {
    console.log(`The string contains ${checkString}`);
} else {
    console.log(`The string does not contain ${checkString}`);
}